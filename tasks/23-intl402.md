# T23 — Complete ECMA-402 Intl implementation

**Status:** In progress — `Intl.getCanonicalLocales` now combines its observable per-index array-like walk with one pinned locale provider/import and pre-instantiation artifact identity gate, and the implemented Intl constructors reserve tagged results before locale/tag/options observation; complete locale/Intl services and artifact data images remain open

**Parallel group:** Feature lane; split by Intl service after shared locale/options foundations  
**Depends on:** T06, T10, T18, T20, T22  
**Blocks:** `intl402` and locale-sensitive T26 closure

## Current repository state

The product Wasm backend has structural locale canonicalization,
`Intl.getCanonicalLocales`, a partial `Intl.Locale`, and a substantial
DateTimeFormat slice. DateTimeFormat currently has one locale (`en-US`),
Gregorian/ISO calendars, `latn`, fixed-offset zones, and handwritten patterns;
it is not the general data-backed service required by ECMA-402. No other Intl
service family is complete. The extensive Intl service code under vendored
Boa/spec-exec is oracle-side and does not count as Wasm-AOT support. The
`intl402` suite is not current-pin green, so this remains one of the largest
open feature lanes.

`lila-intl` owns the shared runtime seam. Its data
identity requires a typed canonical default locale and fixes schema, profile,
placement, digest and upstream versions together. A sealed `IntlHostOp`
catalogue associates locale and time-zone canonicalization with distinct
request/result/error types and required data capabilities. `IntlKernel` accepts
only an identity-matched provider, then grants an operation-specific handle
only when the selected profile proves the operation's capability. Raw strings,
cross-operation requests, profile plans without artifact identity, and
provider/artifact identity mismatches therefore cannot silently enter the
kernel.

The first real product call now crosses that seam. `lila-intl` directly pins
`icu_locale = 2.0.0` and `icu_locale_data = 2.0.0`; a host-embedded,
Locale-only provider exposes the data archive digest and `External` placement
relative to emitted Wasm. `Intl.getCanonicalLocales` performs all observable
element work and structural validation in Wasm. It keeps the original
array-like object, snapshots `length` once, and then performs HasProperty,
conditional Get, coercion, provider canonicalization and deduplication before
advancing each index. Holes are skipped, inherited indices and primitive
wrapper prototypes participate, Proxy `has` abrupt completions propagate, and
an earlier element's coercion can change a later element. Primitive boxing,
null rejection and the result Array prototype use the called builtin's defining
Realm. Only then does the typed `CanonicalizeLocale` operation resolve CLDR
aliases, so `iw-IL` and `he-IL` converge. The shared ABI has typed read/write
spans, stable operation wires, and a closed written/rejected result domain; the
engine rejects unknown operations, malformed memory/UTF-8, output overflow,
provider-data faults, and the deliberately unbound time-zone operation.

The shared Proxy internal-method implementation still creates some of its own
errors in the main Realm. This slice guarantees unchanged propagation of a
user-thrown Proxy `has` sentinel, not correct defining-Realm provenance for
Proxy-generated TypeErrors; that remains owned by the shared object seam.

`Intl.Locale` now performs its observable `OrdinaryCreateFromConstructor`
boundary before checking or coercing the tag. The complete non-copyable
reserved/initialized lifecycle now belongs to the private
`builtins/intl/construction_lifecycle.rs` child. Rust requires its carrier and
transition names to be `pub(super)`, but both tuple fields stay child-private
and the parent constructor retains only inferred state handoffs. The exact
15-line carrier and 97-line transition selections retain visibility-normalized
SHA-256
`f7515bf0b336e4307fac6cdefb699e32b4b3794bd0a6eff9e4f3d58113473725`
and
`7aea2daa1ccd0b8d9bdd8f5ac35eb2287eef2c686037a8e41f226c7ce659d0fa`;
their combined 112-line hash is
`ca374b7f75159c8b7c978d46ee0be44be1faafb9ac34d6b8e686a200ba5d4ac4`,
and the 117-line child hash is
`3ebcef67424bcff990b0e6f6ed519e5c40185288d3b3ab9c21311c9110dc1bd0`.
The unchanged reserve and initialize/publish call hashes are
`3fd56c270a997572d0c093e16d933bc366f4ec3f5371fb20d36835affe92c3d9`
and
`44a6f0a8622f2073daad21bf287597fee7048bd60325c79d7f1606125b4b5b4e`.
The recursive guard pins zero parent production carrier names, four/four child
carrier uses, one/two child projections and one definition plus one parent call
for every transition. `IntlLocaleConstructor` now enters through the closed
direct-returning constructor domain, so the generic construct path cannot read
`NewTarget.prototype` before the lifecycle reserve transition. At the
2026-08-28 Batch X checkpoint, `cargo xc` is green, the exact structure and
registered CLI witnesses each pass `1/1`, and the tag-coercion plus subclassing
leaves pass `4/4` Wasm-AOT executions. The getter-order leaf remains `0/2`
because Locale options are still ignored.

The lifecycle keeps the reserved object unreachable until its structural
locale record and brand are initialized, so a throwing `NewTarget.prototype`
getter prevents tag and options observation while a throwing tag coercion
occurs only after the prototype read. The shared tagged prototype route is
reused, preserving Function, Array, and Arguments prototype identity as well
as its current Realm fallback policy. Locale options are still ignored, and
created-realm Intl bootstrap remains absent, so neither full `UpdateLanguageId`
nor the cross-Realm default-prototype cases are claimed. The only behavior
repair in this owner batch removes the duplicate generic prototype lookup; no
published conformance count changes.

`Intl.DateTimeFormat` now follows the same construction boundary. A private
non-copyable reserved/initialized lifecycle resolves `NewTarget.prototype` and
allocates the tagged ordinary result before locale-list canonicalization,
options coercion, or any option getter. Prototype lookup abrupt completion
therefore wins without observing either argument, while an abrupt locale
coercion occurs only after reservation and still prevents options access.
Function, Array, and Arguments custom prototypes retain their full tag through
allocation, and only the completely populated DateTimeFormat record and brand
can be published. The shared construct dispatcher classifies DateTimeFormat as
direct-returning, so the builtin reservation happens before and instead of the
generic receiver's prototype lookup and preallocation; an explicit NewTarget
is therefore observed exactly once. This slice deliberately preserves the
existing `CurrentGlobal` fallback: created-realm Intl bootstrap and the
complete active-function/cross-Realm fallback rules remain open. The boundary
now belongs to the private
`builtins/intl_datetimeformat/construction_lifecycle.rs` child. Rust requires
the carrier and transition names to be `pub(super)`, but both tuple fields stay
private and the parent constructor retains only inferred state handoffs. The
exact 15-line carrier and 74-line transition selections retain
visibility-normalized SHA-256
`da0ef9cbc1c427a53a63bb0234432393a614beef69c9c458943f39d856d1ede9`
and
`0a20691937370549c0cf98a0216a232ba8340a320aeeadaae2358dc6b273a391`.
The 94-line child has SHA-256
`8a99ba9d18ad16e4d2321fd74e7c54b25d9646c46bd7a654139f9762224c650d`
and reduces the concurrent parent from 7,182 to 7,093 lines. The retained
reserve and initialize/publish call hashes are
`3c4063c6330e34afc29af4509b19410c24c4acaad337eb41084a0961b1256220`
and
`c11b51e3dec4d4c0b9018cc64a875d508cc465e48d4edc1d4487695b93d0bb27`.
Its exact eight-file ownership list is frozen by
`docs/rust-rewrite/contracts/intl-date-time-format-construction-order.md`.
At the 2026-08-28 Batch W checkpoint, `cargo xc` is green, the recursive
structure target passes `1/1`, and the exact construction-order CLI fixture
passes `1/1`. Pinned Intl Test262, semantic snapshot and broad verification
were not rerun; no behavior or conformance change is claimed.

The shared DateTimeFormat range body now accepts only its closed
`DtfFormatMode` instead of an independently supplied method-name string. An
exhaustive projection binds `String` to `formatRange` and `Parts` to
`formatRangeToParts`, so a caller cannot combine one API's receiver diagnostic
with the other API's formatter and result tag. The two public emitters are the
only callers and pass one named mode each. Receiver branding still precedes
both argument conversions, and the existing exhaustive String/Array result-tag
selection is unchanged. The bounded structure target passes `3/3`; `cargo xc`
is green, and the 647-artifact Wasm golden has an empty recursive pre/post diff.
The direct pinned Intl leaves remain outside this typed-boundary checkpoint; no
conformance change is claimed.

The same `DtfFormatMode` now derives only its required `Clone` and `Copy`
capabilities. Its last equality observation, the parts-array allocation gate,
is an exhaustive `String`/`Parts` match that preserves the exact capacity,
allocation and buffer-load sequence. A future output mode can no longer inherit
String's no-allocation behavior through an equality default. The bounded guard
pins the declaration, recursive ownership and variant censuses, and complete
allocation projection; the focused evidence and nonclaims are recorded in
[`intl-date-time-format-mode-capability.md`](../docs/rust-rewrite/contracts/intl-date-time-format-mode-capability.md).
This is source-equivalent capability closure, not new Intl behavior or a T23
completion claim. The dedicated and neighboring range-mode structure targets
pass `3/3` each. The exact
`intl402/DateTimeFormat/prototype/formatToParts/main.js` witness passes both
sloppy and strict Wasm-AOT executions (`2/2`), with every failure bucket zero
and both outcomes `Success`. Targeted guard formatting and the lane diff check
are clean, and the package-wide format check is green. Independent review
confirmed the required copy capabilities, removed equality surface, recursive
census and complete allocation projection. The coordinated workspace
checkpoint passes `cargo fmt --all -- --check`, `cargo xc`, `git diff --check`,
the module boundary check and the task-plan check; the compile retains the
repository's existing warnings.

All five initialized-DateTimeFormat receiver checks now accept the private,
non-copyable `IntlDateTimeFormatReceiverOperation` domain instead of a raw
method string. One borrowed exhaustive projection owns the complete diagnostics
for `resolvedOptions`, the `format` getter, `formatToParts`, `formatRange` and
`formatRangeToParts`; its ordered `ALL` list also drives the existing string
pool order. The three single-value entry points construct named operations
directly, while the range body's exhaustive `DtfFormatMode` mapping selects the
two range operations before the unchanged brand-first argument boundary. The
focused ownership is recorded in
[`intl-date-time-format-receiver-operation.md`](../docs/rust-rewrite/contracts/intl-date-time-format-receiver-operation.md).
At the 2026-08-27 focused checkpoint, the receiver and neighboring range-mode
structure targets pass `4/4` and `3/3`, the retained construction-order CLI
fixture passes `1/1`, and the six exact receiver leaves pass all `12/12`
sloppy/strict Wasm-AOT executions. Every Parser, EarlyError, Lowering, Runtime,
WasmBackend, HostHarness and Unsupported bucket is zero; all twelve outcomes
are `Success`, with `NotImplemented`, `Crash` and `Bug` also zero. This remains
a source-equivalent invariant checkpoint, not a full DateTimeFormat, Intl402 or
T23 closure claim, and it changes no published conformance count.

The DateTimeFormat range formatter now keeps its component, range-local and
closed range-pattern carriers non-cloneable and non-copyable. Eight pre-release
observations share the sole optional `DtfRangeLocals`; the final tail alone
consumes it, releasing the selected-pattern/practical-equality/loop locals and
then the end and start component sets in reverse order. The caller-owned
`second_time` local remains unreleased. The exact 11/4/8 ownership census,
producers, helper borrows and complete exhaustive consuming tail are recorded in
[`intl-date-time-format-range-locals-ownership.md`](../docs/rust-rewrite/contracts/intl-date-time-format-range-locals-ownership.md).
The emitted formatter selects the CLDR `en` textual year/month/day interval
patterns only for that exact effective field set, sharing the year suffix and,
for day-only differences, the month prefix. Numeric and mixed date/time formats
retain the complete-side fallback. At the focused Wasm-AOT checkpoint, the
same-date and en-US range leaves for both APIs pass all `8/8` sloppy/strict
executions. Every Parser, EarlyError, Lowering, Runtime, WasmBackend,
HostHarness and Unsupported bucket is zero; all eight outcomes are `Success`,
with `NotImplemented`, `Crash` and `Bug` also zero. This remains a focused
range-pattern checkpoint, not a full DateTimeFormat or T23 closure claim.

The five `Intl.Locale` string-returning entry points now select a closed
`IntlLocaleStringSlot` instead of independently supplying a heap offset and an
optionality boolean. Exhaustive projections bind each named slot to its record
offset and restrict absent-to-`undefined` behavior to `script` and `region`;
`tag`, `language`, and `baseName` remain required strings. The bounded structure
target passes `3/3`; `cargo xc` is green, and the 647-artifact Wasm golden has
an empty recursive pre/post diff. The direct `getters.js`/`getters-missing.js`
leaves remain outside this typed-boundary checkpoint. No conformance change is
claimed.

That slot authority is now private and non-derived, its projections borrow it,
and the raw emitter is private behind five fixed catalog entries. The
[`string-slot dispatch contract`](../docs/rust-rewrite/contracts/intl-locale-string-slot-dispatch.md)
records exact source-equivalence SHA-256 witnesses
`00486705af5ad3a89c1386f4ca8b3088d5531ca676a582aa643ca90bca658d6a`
and
`4b346dcd2c819c503603ed7c08842e577d4b893dc98aa1f33c5f7d2c864cd134`.
Its structure target passes `4/4`, the two neighboring structure targets pass
`8/8`, and the exact canonical-tag roles CLI control passes `1/1`. This is
source-equivalent hardening with no new Intl behavior and does not close T23.

The defining-Realm ToObject and result-Array paths are source-structural and
independently reviewed, not runtime-proven across realms. Created realms do not
yet install `Intl`, so a foreign `Intl.getCanonicalLocales` witness is
unreachable. T06/T23 created-realm Intl bootstrap must land before runtime tests
can prove foreign Number wrapper lookup, foreign null TypeError identity and
foreign `%Array.prototype%` result identity.

Every emitted module that imports `lila_host.intl_call` now carries exactly one
`lila.intl-data-identity.v1` custom section. `lila-intl` alone serializes the
complete identity canonically; the emitter cannot construct a partial metadata
record, and the engine compares those canonical bytes to the shared
`EmbeddedLocaleProvider` identity before Wasmtime compilation or
instantiation. Missing, duplicate, mismatched, and import-less unexpected
sections are typed engine errors. Since the identity lives in the Wasm bytes,
program and module caches preserve the gate without parallel cache metadata.

This remains a narrow slice. `Intl.Locale` is intentionally not routed through
the provider because its component slots still come from the structural pass;
doing only the tag would make the object internally inconsistent. There is no
generated artifact data image, artifact-embedded ICU payload, general locale
matching, or time-zone/provider integration yet. The carried identity describes
the external host provider and enforces its digest; it is not the future
`Embedded` data image. The central feature-enabled CLI compile is green, as are
focused contracts for canonical artifact encoding, typed host-import selection,
pre-instantiation identity rejection and locale-alias resolution before
deduplication. No full-tree green count is claimed here.

The time-zone operation is not merely waiting for a linker arm. The resolved
`timezone_provider 0.1.2` canonicalizer identifies its baked alias data as
tzdb `2025b`, while the shared transition line promised by T22/T23 is `2026a`.
Binding those together would advertise an identity the inputs do not have, so
the operation stays explicitly unavailable until one coherent catalogue owns
both canonical names and transitions.

The shared structural canonicalizer now accepts one private, move-only
canonical locale tag invocation authority with distinct input, tag, language,
script, region, base-name, and validity roles. Its five producers can no longer
transpose tag, language, script, region, base-name, and validity roles while
continuing to compile; the canonicalizer alone consumes and projects the
authority. The Rust-lexical
`intl_canonical_locale_tag_invocation_structure` guard owns the closed census,
all complete producers and the sole projection, while the focused public CLI
fixture observes the `Intl.Locale`, `Intl.getCanonicalLocales`, and
DateTimeFormat result roles. This is a source-equivalent invariant checkpoint,
not new locale data, an Intl402 closure, or a conformance-count change; the
focused verification record lives in
[`intl-canonical-locale-tag-invocation-authority.md`](../docs/rust-rewrite/contracts/intl-canonical-locale-tag-invocation-authority.md).

The shared DateTimeFormat field walk now keeps its owner-private `DtfFormatMode`
and owner-private `DtfFormatTimes`, carrier fields, and raw
formatter inside `intl_datetimeformat.rs`. Only the three existing fixed
single-value, parts, and range producers can construct that policy. The updated
[`mode capability contract`](../docs/rust-rewrite/contracts/intl-date-time-format-mode-capability.md)
records the exact original SHA-256 witnesses
`2a3472c7bf2f6ea58fdca16c0e3aa06afb5e8d6dcbaaab9f96f5991939d8ab70`,
`672ab8ac8cb31e3e580d22705dadd2707e3b06f4c0d7e567d56ec08c1c42cafb`
and
`76bff3098cbab0ded80001f3b2a4687a927045c5992f8cd31ac3f9976a471ae8`.
The mode, range and receiver structure targets pass `3/3`, `3/3` and `4/4`;
the exact `formatToParts/main.js` leaf passes both Wasm-AOT executions with
every failure bucket at zero, and `cargo xc` is green.
This source-equivalent hardening has no new Intl behavior or conformance claim.

The relevant-extension owner now also keeps its keyword-needle carrier private.
The sole constructor binds a key and accepted spelling before the bounded tag
scanner can borrow its bytes and length. The original 22-line carrier has
SHA-256
`ab8dfe30ef006fe674cf1ba6663072d923941c92b568bb8825a15c29ccb4b8a2`.
The strengthened privacy target passes `3/3`; the exact calendar extension
leaf passes both Wasm-AOT executions with every failure bucket at zero, and
`cargo xc` is green.
This source-equivalent hardening has no new Intl behavior or conformance claim.

The owner-private `IntlDtfOption`, its fields, the component table, and the
hour-cycle/date-style/time-style rows now stay inside `intl_datetimeformat.rs`.
The focused
[`option privacy contract`](../docs/rust-rewrite/contracts/intl-date-time-format-option-privacy.md)
records the exact original 95-line SHA-256 witness
`8a430fd40eae20d6975444489d14d9c6d4ef75deaabe9bacde5bd8fce382dc0f`.
The recursive privacy target passes `2/2`; the exact general component-order
and date/time-style order leaves pass all four Wasm-AOT executions with every
failure bucket at zero, and `cargo xc` is green.
This source-equivalent hardening has no new Intl behavior or conformance claim.

The owner-private `TimeZoneNameStyle` now confines all six DateTimeFormat
time-zone-name styles and their spelling, UTC-name, and heap-code projections
to `intl_datetimeformat.rs`. The focused
[`time-zone-name style privacy contract`](../docs/rust-rewrite/contracts/intl-date-time-format-time-zone-name-style-privacy.md)
records the exact original 66-line SHA-256 witness
`ee5ac6a3396cdf58e102796ca82dbc6c75bf2799fe8c93bc3c42f17d091ea117`.
The recursive style target passes `3/3`; the exact six-style constructor leaf
passes both Wasm-AOT executions with every failure bucket at zero, and
`cargo xc` is green.
This source-equivalent hardening has no new Intl behavior or conformance claim.

The DateTimeFormat Temporal-kind family is now confined to its sole source
owner. The owner-private `IntlDtfTemporalKind`, owner-private `DtfTimeBasis`,
six-row table, owner-private `DtfBrandedKind`, and owner-private `DtfValueKind`
cannot be constructed or selected by another backend module. The focused
[`Temporal-kind privacy contract`](../docs/rust-rewrite/contracts/intl-date-time-format-temporal-kind-privacy.md)
records the exact original SHA-256 witnesses
`d6a9458aa55ec9362cf9fb4481717b58c78f7d4a5bc856d3b377444c853a5d7e`
and
`72754a52a21a06e8db39307da0b08b43ae0d57ec28e5f7cc212e0ca1e032ee40`.
The recursive privacy target passes `3/3`; the exact PlainDate table/basis and
ZonedDateTime branded-rejection leaves pass all four Wasm-AOT executions with
every failure bucket at zero, and `cargo xc` is green. The separate resolved
named-zone leaf remains blocked by the existing unsupported time-zone boundary.
This source-equivalent hardening has no new Intl behavior or conformance claim.

DateTimeFormat's relevant-extension domains are now confined to their sole
source owner. The owner-private `IntlDtfRelevantExtensionKey`, its private
complete list, and the owner-private `IntlDtfExtensionResolution` cannot be
constructed or reordered by another backend module. The focused
[`extension-key privacy contract`](../docs/rust-rewrite/contracts/intl-date-time-format-extension-key-privacy.md)
records the exact original 122-line SHA-256 witness
`81c65d3e0cba0940b53421102caa0536bb0820d0acbe531d75abbeb8f555274e`.
The recursive privacy target passes `3/3`; the exact calendar canonical-string
and hour-cycle code resolution leaves pass all four Wasm-AOT executions with
every failure bucket at zero, and `cargo xc` is green.
This source-equivalent hardening has no new Intl behavior or conformance claim.

DateTimeFormat's fixed-offset catalogue and resolved-zone lifecycle now stay
inside their sole source owner. The owner-private `TzOffsetMinutes`,
owner-private `IntlDtfNamedZone`, and private named-zone table cannot be
constructed by another backend module. The owner-private `DtfCanonicalTimeZone`
cannot be reserved there, and the owner-private `DtfResolvedTimeZone` cannot be
published there. The focused
[`time-zone authority privacy contract`](../docs/rust-rewrite/contracts/intl-date-time-format-time-zone-authority-privacy.md)
records the exact original SHA-256 witnesses
`4f284353c06da9e135d8ff5e863a7310b4abc94a7e1fcc57076be898e54cf641`
and
`6a1a4427fae20803f35d9b3a35c62f12a1ee3224259e2a620365326b358c7513`.
The focused structure target passes `4/4`; the exact UTC-name and fixed-offset
equivalence leaves pass all four Wasm-AOT executions with every failure bucket
at zero, and `cargo xc` is green.
This source-equivalent hardening has no new Intl behavior or conformance claim.

## Objective

Implement the pinned ECMA-402 revision in the Rust/Wasm product path with reproducibly pinned locale, Unicode and time-zone data. Do not count delegation to Boa's spec-exec engine as AOT Intl support and do not depend on the embedding process locale for observable results.

## Architecture and data strategy

[`docs/rust-rewrite/intl-architecture.md`](../docs/rust-rewrite/intl-architecture.md)
is the source of truth. It selects:

- an exactly pinned ICU4X 2.0 line with CLDR 47.0.0, Unicode 16.0.0, ICU data
  tag `icu4x/2025-05-01/77.x`, segmenter data v0.1.0, and shared IANA tzdb
  2026a as the first complete conformance data line;
- a versioned immutable data image with `Conformance`, `Minimal`, and validated
  custom profiles, embedded by default or supplied through an explicit
  digest-matched embedder capability;
- one time-zone identifier/transition catalogue shared with T22, with localized
  zone names layered on top rather than a second transition source;
- an emitted-Wasm observable shell followed by a typed Rust Intl kernel that
  accepts only fully observed, canonicalized primitive records; and
- closed service, host-operation, profile, placement, and data-capability
  domains so incomplete profiles and cross-service handles are rejected by
  construction.

The existing `lila_host` mechanism is the initial pure-kernel transport, but it
may not call host-native Intl or inspect JavaScript objects. Artifact/provider
schema or digest mismatch is rejected before Wasmtime instantiation through the
artifact-carried identity. The default locale and all formatting data come from
the selected artifact profile, never the process or operating system.

The default conformance build must include every locale/data item needed by the pinned Test262 suite. Missing data must produce specification-compliant fallback, not an internal error or English-only shortcut.

## Implementation order and parallel seams

1. Complete the shared `lila-intl` foundation. The typed data identity, exact
   locale pins, host-embedded locale kernel and first consumed Wasm ABI
   operation have landed. Canonical artifact identity metadata and
   pre-instantiation provider matching have also landed. Deterministic artifact
   image generation/validation and the remaining operation bindings remain.
2. Finish shared locale/options operations and replace the bootstrap locale
   tables. This owns canonicalization, matching, extensions, Locale and
   `supportedValuesOf`.
3. Implement services concurrently behind service-specific validated request
   and result types: NumberFormat/PluralRules, DateTimeFormat/T22 integration,
   Collator/Segmenter, and ListFormat/DisplayNames/RelativeTime/DurationFormat.
4. Integrate locale-sensitive core builtins, remove the bootstrap-only tables,
   measure conformance/minimal profiles separately, and run the full closure
   gates.

Service files are parallel-owned. The locale/options layer, data manifest,
host-operation catalogue, intrinsic registry and artifact ABI are
integrator-owned shared files.

The passive Intl.DateTimeFormat heap layout now uses a closed
`IntlDateTimeFormatHeapSlot` identity domain. One private exhaustive projection
owns all twenty-three names, offsets, widths and pointer classifications; the
typed registry fixes their order and preserves the six-pointer/seventeen-scalar
census. The former 165-line table and new 257-line owner have SHA-256
`7c2284a3fc1325cf43f042d1df6240f96b1c273836bada75c9cd2a8410d7d6a9`
and `4679b3d4ffae6088c8dca5c580b8356278e91b64624e965b6ef6270a9cb5dd59`.
The focused contract is
[`intl-date-time-format-heap-slot-authority.md`](../docs/rust-rewrite/contracts/intl-date-time-format-heap-slot-authority.md).
Allocation, field access, formatting, emitted Wasm, root scanning and collector
execution remain untouched. At the 2026-08-28 Batch AR checkpoint, `cargo xc`
is green, the structure target passes `4/4`, the focused slot-identity unit
passes `1/1`, and both heap-layout registry controls pass `2/2`. No runtime CLI,
Test262 leaf or semantic golden is required. This source-equivalent
passive metadata migration claims no new Intl behavior.

## Shared Intl abstract operations

Implement one common layer for:

- structurally valid language tags and Unicode locale identifiers;
- canonicalization, grandfathered/deprecated aliases and extension handling;
- `CanonicalizeLocaleList` with array-like length, HasProperty, getter and
  coercion ordering;
- locale lookup/best-fit matching and supported-locales results;
- options object conversion and `GetOption`/`GetNumberOption` behavior;
- Unicode extension key negotiation;
- numbering systems, calendars, collations, hour cycles and time zones;
- `resolvedOptions` object creation and ordered properties;
- shared constructor call/new-target and legacy-constructor behavior where specified.

All JavaScript-visible reads/calls must use T04/T10. ICU inputs may be prepared only after observable access and validation are complete.

## Service families

Implement every constructor/prototype/API present in the pinned suite, including as applicable:

- `Intl.Collator`;
- `Intl.NumberFormat`, range formatting and format-parts APIs;
- `Intl.DateTimeFormat`, range formatting, format-to-parts, calendars and time zones;
- `Intl.PluralRules` and range selection;
- `Intl.RelativeTimeFormat`;
- `Intl.ListFormat`;
- `Intl.DisplayNames`;
- `Intl.Segmenter` and segment iterator/containing behavior;
- `Intl.Locale`, maximize/minimize and locale-info accessors;
- `Intl.DurationFormat`;
- `Intl.supportedValuesOf`;
- locale-sensitive String, Number, BigInt, Date and Temporal methods.

Track the pinned ECMA-402 revision rather than assuming the list above is complete.

## Object and realm semantics

- Install exact constructors, prototypes, accessors, method names/lengths and descriptors from the intrinsic registry.
- Enforce internal-slot branding for extracted methods and cross-realm receivers.
- Implement bound formatter getter identity/caching rules.
- Preserve subclass/custom-new-target behavior where constructors permit it.
- Keep resolved option values and property ordering exact.

## Formatting correctness

- Use pinned CLDR/Unicode data and expose expected canonical aliases.
- Handle NaN, infinities, signed zero, BigInt and extreme Date/Temporal values.
- Preserve required rounding modes, increments, notation, sign display, grouping and currency/unit rules.
- Ensure parts arrays and range source annotations have exact structures/descriptors.
- Segmenter indices are UTF-16 code-unit indexes, not UTF-8 byte offsets.

## Performance and artifact size

Add benchmarks for constructor setup, cached formatting, segmentation and artifact/data size. Caching is allowed only when user-observable option/property reads have completed and realm/object identity rules are preserved. Report the conformance build's data footprint separately from a minimal production profile.

## Acceptance criteria

- The complete pinned `intl402` tree is green under Wasm-AOT.
- All locale-sensitive methods in core builtins pass their adjacent Test262 filters.
- Results are reproducible across supported hosts with the same pinned data.
- Locale/options access-order tests pass with proxies, accessors and thrown values.
- No service silently falls back to host locale formatting or an English-only handcrafted formatter.
- Constructors, bound functions, branding, descriptors and cross-realm tests pass.
- Data version and build profile are visible in diagnostic/artifact metadata.

## Required tests

```sh
cargo test -p lila-intl --quiet
cargo test -p lila-aot-wasm intl_ --quiet
cargo test -p lila-engine wasm_intl_canonical_locale_aliases_are_resolved_before_deduplication --quiet
cargo test -p lila-cli wasm_intl --quiet
./target/debug/lila test262 run intl402 --execution-backend wasm-aot --timeout-ms 240000 --threads 4
```

During development run one service subtree at a time and compare selected outputs across at least Linux and another supported host using identical pinned data.
