[package]
name = "porffor-spec-exec"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
boa_engine = { version = "0.21.0", features = ["annex-b", "experimental", "intl_bundled"] }

[lib]
path = "src/lib.rs"
